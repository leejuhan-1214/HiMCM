# BeeLab · 모델 설명과 보고서 작성 메모

이 문서는 2022 HiMCM Problem A를 위한 웹 시뮬레이터의 범위, 근거, 해석 방법을 정리한다. 모델은 **한 벌통의 일별 기대 개체수**를 계산하고 그 결과를 20에이커의 수분 방문 수요에 연결한다. 관측 자료에 적합시킨 현장 예측 모델은 아니다. 화면의 숫자·순위·범위는 선택한 가정에서 다시 계산되는 모의 결과이다.

## 1. 대회 요구사항과 구현의 연결

| 문제 요구사항 | 웹 시뮬레이터에서 확인할 내용 | 보고서에서 설명할 내용 |
|---|---|---|
| 시간에 따른 군집 크기 | 연간 개체군 곡선과 단계별 개체수 | 재귀식, 상태 정의, 초기조건, 발달 지연 |
| 민감도 분석 | 파라미터 변화에 따른 출력 변화 | 변화 범위, 정규화 방법, 순위의 한계 |
| 20에이커에 필요한 벌통 수 | 개화기 채집벌 공급과 꽃 방문 수요 | 작물·지역 가정, 날짜별 부족, 경험값과 비교 |
| 비전문가 대상 자료 | 1페이지 블로그 화면 | 어려운 수식 없이 결과와 의미 전달 |

공식 문제는 요약시트·목차·참고문헌·부록을 포함한 **25페이지 이내 PDF**를 요구한다. 웹사이트와 코드만으로 PDF 제출물이 완성되는 것은 아니다. [COMAP 문제 원문](https://www.contest.comap.org/highschool/contests/himcm/2022_Problems/2022_HiMCM_Problem_A.pdf)

## 2. 근거와 시나리오 가정을 구분하기

| 항목 | 기본 해석 | 근거의 범위 |
|---|---|---|
| 알 3일, 유충·번데기 18일 | 일벌의 21일 발달 기준 | FAO의 대표 발달기간. 개체·조건에 따른 변이는 생략 |
| 알 단계 생존율 0.942 | 3일 전체를 통과할 확률 | Fukuda & Sakagami의 벌집 중앙부 관찰 예시 |
| 유충·번데기 생존율 0.851/0.942 | 부화 후 우화할 조건부 확률 | 위 관찰에서 초기 알 대비 누적 생존율을 환산 |
| 최대 산란량 | 건강한 군집의 산란 능력을 가정 | 1,500–2,000개/일 범위는 FAO 설명과 연결. 계절곡선은 자체 가정 |
| 사회적 억제 | 채집벌 비율이 높을수록 전환 억제 | Khoury 2011의 구조적 아이디어를 변형 |
| 양육 보정 | 내근벌 부족 시 유지되는 알 감소 | Khoury 2013의 양육 제한 개념을 단순화 |
| 수벌 24일 발달 | 미수정란을 별도로 추적 | FAO. 수벌의 생존·사망 세부값은 자체 가정 |
| 성충 수명·계절·초기 개체수 | 사용자가 변경하는 기준 시나리오 | 단일 현장 자료로 추정하지 않음 |
| 꽃 밀도·방문 요구량·개화기 | 작물별 예시 입력 | 실측값이 아닌 변경 가능한 시나리오 |
| 작물별 벌통/에이커 | 별도의 현실 비교 지표 | OSU의 태평양 북서부 평균 사용량 |

FAO의 발달·산란 설명은 [Honeybee biology](https://www.fao.org/4/x0083e/X0083E03.htm)를 참고한다. 1968년 관찰에서 부화 생존은 0.942, 산란부터 성충까지의 누적 생존은 0.851이었다. 따라서 0.851을 다시 유충 생존확률로 곱하면 사망을 중복 반영한다. 같은 연구에서는 성공적으로 우화한 개체의 다수가 총 20일의 발달기간을 보였다. 그러므로 통합 유충·번데기 17일을 항상 오류라고 단정할 수 없으며, 여기서는 일반적인 18일을 기준으로 선택한다. [Fukuda & Sakagami 1968](https://doi.org/10.1007/BF02514731)

양육 보정은 이론적 추가 가정이다. 역사적 관찰 생존율과 양육 보정을 함께 적용한다고 해서 자동으로 현장에 보정되는 것은 아니다. 예시 생존율을 충분한 양육 조건의 기준으로 사용한 뒤, 부족한 양육에 대한 추가 손실을 탐색한다. 양육 보정의 크기 역시 민감도와 관측 자료로 검토해야 한다.

## 3. 상태와 하루의 정의

내부 배열 `E`는 일벌 알, `B`는 일벌 유충·번데기, `H`는 성충 내근벌, `F`는 성충 채집벌이다. `D`는 성충 수벌이며 미성숙 수벌은 `DE`, `DB` 대기열에 보관한다. **화면과 일별 출력의 `eggs`는 E+DE, `brood`는 B+DB**의 합계이다. 여왕벌 한 마리의 생존·교체는 상수 조건으로 두고 개체군 합계에서 제외한다. 개체수는 확률적 개체 수의 기대값으로 계산하므로 소수가 나올 수 있다.

서로 다른 합계는 구분해 해석한다.

- 성충 일벌: `N_worker = H + F`
- 전체 성충: `N_adult = H + F + D`
- 전체 발달단계: 일벌 알·유충·번데기, 미성숙 수벌, 전체 성충의 합
- 수분에 이용되는 인력: 그날 활동할 수 있는 `F`에 활동·날씨·작물 선택 보정을 적용한 값

성충 수를 알·유충까지 포함한 값과 직접 비교하지 않는다. 같은 이유로 성충 일벌 수와 개화기 채집벌 누적량은 서로 다른 민감도 결과를 낼 수 있다.

알과 유충·번데기는 나이별 배열로 하루씩 이동한다. 단계 전체 생존확률 `S`, 체류기간 `d`에 대해 일일 생존확률은 `s = S^(1/d)`이다. 이 일일 확률을 정확히 `d`번 적용하고, 단계 끝에서 전체 생존확률을 다시 곱하지 않는다. 새 알이 기록되는 시각과 우화 시각의 차이가 정확히 21일인지 충격 입력 검사로 확인한다.

H→F 전환은 같은 시점의 H와 F를 이용해 계산한다. 성충 사망과 전환의 순서를 정하고, 전환 수가 그날 생존한 H를 넘지 않도록 한다. 내부 전환은 총 개체수를 바꾸지 않으며, 총 개체수 변화는 새로 유입된 알에서 사망을 뺀 값과 일치해야 한다.

## 4. 계절·양육·역할 전환

연중 날짜를 입력으로 0–1 사이 계절지수를 만들고 산란량과 활동도를 조절한다. 기온 관측자료를 이용한 지역별 예측이 아니므로 특정 기후대에서 검증됐다고 표현하지 않는다. 윤년은 제외하고 1년을 365일로 둔다. 아래는 `src/model.js`의 실제 계산식이며 `clip(x, a, b)`는 x를 a와 b 사이로 제한한다.

```text
doy(t) = (t mod 365) + 1
w(t) = [1 + cos(2π (doy(t) − peakDay) / 365)] / 2
A(t) = clip((w(t) − 0.12) / 0.88, 0, 1)
Q(t) = maxLaying × w(t)^seasonPower
g(H) = H / (H + careHalf)
```

상태는 매일 시작 시각의 값이다. t→t+1 갱신에서 새로 입력되는 일벌 알은 `I_w(t+1) = Q(t+1) × g(H_t) × workerRatio`, 수벌 알은 `I_d(t+1) = Q(t+1) × g(H_t) × (1−workerRatio)`이다. 다음 날짜의 산란 잠재량에 현재 내근벌의 양육능력을 적용한다. 초기 t=0 알은 `Q(0) × g(H_0)`에서 시작한다. 날짜 t에 입력된 알은 t+21에 일벌 성충, t+24에 수벌 성충으로 우화한다.

```text
E_0(t+1) = I_w(t+1)
E_a(t+1) = sE × E_(a−1)(t),  a = 1, 2
B_0(t+1) = sE × E_2(t)
B_a(t+1) = sB × B_(a−1)(t),  a = 1, …, 17
Y(t+1) = sB × B_17(t)

sE = eggSurvival^(1/3)
sB = (broodSurvival × nutrition)^(1/18)
```

수벌의 `DE`도 3칸이다. `DB`는 21칸으로, 일일 생존확률은 `(broodSurvival × nutrition)^(1/21)`을 사용한다. 일벌과 수벌에 같은 단계 생존확률을 주는 것은 자료로 검증하지 않은 단순화이다. `nutrition`은 단계 생존확률에 한 번 작용하는 외생적 감쇠계수이며, 꽃가루 섭취량이나 저장량을 측정해 환산한 값이 아니다.

내근벌 양육 보정의 목적은 산란 잠재량과 실제로 모델에 유지되는 알을 구분하는 것이다. 사회적 억제는 기존 채집벌이 새 채집벌 모집을 늦추는 장치로 사용한다. 최소 성충 나이보다 어린 H는 F로 전환하지 않으며, 이후에는 나이에 따른 성숙도와 군집 구성이 전환을 함께 조절한다. 이 구현은 Khoury의 연속시간 모델을 그대로 옮긴 것이 아니다. [Khoury et al. 2011](https://doi.org/10.1371/journal.pone.0018491), [Khoury et al. 2013](https://doi.org/10.1371/journal.pone.0059084)

성충 갱신에 쓰는 정확한 식은 다음과 같다. `a`는 성충 일령이고, `H = Σ_a H_a`이다.

```text
fraction = F / (H+F)                       [H+F=0이면 0]
social = max(0, 1 − socialInhibition × fraction / 0.35)
maturity(a) = 0                            [a < minimumForagerAge]
            = 1 / (1 + exp(−(a−transitionAge)/3))  [나머지]
rho_a(t) = clip(0.35 × A(t) × maturity(a) × social, 0, 1)

muH(t) = A(t) × hiveMortality + (1−A(t)) / winterLife
muF(t) = A(t) / summerForagerLife + (1−A(t)) / winterLife
U_a(t) = (1−muH(t)) × H_a(t) × rho_a(t)

H_(a+1)(t+1) = (1−muH(t)) × H_a(t) − U_a(t)
H_0(t+1) = Y(t+1)
F(t+1) = (1−muF(t)) × F(t) + Σ_a U_a(t)
```

H의 마지막 칸은 365일령 이상 개체를 누적하여 배열 길이 때문에 벌이 사라지지 않게 한다. 새로 우화한 일벌은 다음 경계에서 H_0에 들어가므로 같은 갱신에 바로 채집 전환·성충 사망을 적용하지 않는다. 성충 수벌의 사망확률은 `muD(t) = A(t)/30 + (1−A(t))×0.12`로 두고 미성숙 수벌의 우화를 더한다. 30일과 0.12는 수벌의 계절적 감소를 표현하는 자체 가정이다.

기본값은 `maxLaying=1900`, `workerRatio=0.95`, `careHalf=2500`, `peakDay=170`, `seasonPower=1.3`, `transitionAge=18`, `minimumForagerAge=8`, `socialInhibition=0.75`, `hiveMortality=0.003`, `summerForagerLife=12`, `winterLife=150`, `nutrition=1`이다. 0.35와 로지스틱 폭 3도 미보정한 함수 선택이다. `transitionAge`는 로지스틱 성숙도가 절반인 나이로, 실제 평균 채집 시작 나이를 뜻하지 않는다.

겨울에는 활동과 신규 채집 전환을 줄여 H에 더 오래 머물도록 한다. 기존 F는 경험 있는 채집벌 구획에 남지만 실제 활동 인원은 `activeForagers = F × A(t)`로 따로 계산한다. F→H 역전환은 구현하지 않는다. 기본 H 일일 사망확률은 성수기의 0.003보다 겨울의 1/150이 높다. 따라서 장수 효과를 단순히 “겨울 H 사망확률 감소”라고 설명하면 안 되며, 위험한 채집 단계로의 이동 억제까지 함께 해석한다.

`winterLife = 150`이라는 값은 모든 벌이 정확히 150일에 죽는다는 뜻이 아니다. 사망확률이 일정한 이산 기하분포를 전제로 `mu=1/lifetime`를 적용한 기대 체류시간의 척도이다. 연중 사망확률이 바뀌거나 H→F로 이동하면 전체 기대수명은 별도로 계산해야 한다. 겨울벌의 생리적 상태를 별도 상태변수로 추적하지 않는 단순화가 남는다.

## 5. 예열과 검증

기본 초기 성충 일벌은 20,000마리이며 내근벌 96%, 채집벌 4%로 나눈다. H는 0–119일령에 균등 배분하고, 초기 brood·수벌 성충은 0으로 두는 가정이다. 초기 알은 위의 양육 제한 산란식으로 넣는다. 기본 예열 2년 후 1년을 출력한다.

예열기간은 임의 초기 연령분포의 영향을 줄이기 위한 장치이다. 여러 해를 돌렸다는 사실만으로 안정 주기에 도달했다고 단정하지 않는다. 마지막 두 해의 동일 날짜 성충 수 차이의 최댓값을 출력기간의 최대 성충 수로 나눈 진단값을 제공한다. 5% 이하는 이 구현의 수치적 허용기준이며 현장 오차 보장이 아니다. 초기 개체수·예열기간을 바꾸어 주요 결론이 유지되는지 확인한다. 붕괴하는 시나리오에서 긴 예열은 이미 붕괴한 군집을 보여 줄 수 있다.

구현 검증에 필요한 항목은 다음과 같다.

- 모든 상태가 유한하고 음수가 되지 않는가.
- 동일 설정으로 동일 결과를 다시 얻는가.
- 단일 산란 입력이 알·유충 대기열을 예정된 날짜에 통과하는가.
- 단계 생존율이 누적 확률과 일치하는가.
- H→F 전환이 전체 성충 수를 생성하거나 제거하지 않는가.
- 새 알과 사망을 합친 일별 개체수 보존 잔차가 수치 오차 범위에 있는가.
- 산란 0, 수정란 비율 0, 채집 불가능 등 극단조건을 계산할 수 있는가.

일별 보존식의 새 유입은 `viableLaying`이다. 잠재 산란 `laying`과 그 차이는 모델 진입 전 양육 제한으로 제외한 것이며, 모델에 이미 들어온 개체의 사망 `deaths`에는 포함하지 않는다. 일별 흐름은 직전 경계에서 현재 경계로 이동한 양을 나타낸다.

이 검사는 계산의 일관성을 확인한다. 현장 검증에는 같은 지역의 날짜별 성충·brood 관측과 파라미터 추정, 독립 관측기간의 오차평가가 별도로 필요하다. 20,000–80,000 범위에 맞게 결과를 강제로 잘라내는 것은 검증이 아니다.

## 6. 민감도 결과 읽기

현재 설정에서 파라미터 하나를 변화시키고 나머지는 고정한다. 기준 출력 `Y0`, 하한·상한 설정의 출력 `Y−`, `Y+`를 사용하면 변화율은 `100 × (Y± − Y0)/Y0`이다. 비교용 정규화 민감도는 `((Y+ − Y−)/Y0) / ((θ+ − θ−)/θ0)`로 계산할 수 있다. 확률은 0–1 범위를 지켜야 하므로 상·하한이 잘린 경우 실제 변화폭을 사용해야 한다. 기준값이 0이면 이 비율은 정의되지 않으므로 절대 변화량으로 표시한다.

순위는 선택한 출력·기준값·변화폭에 대한 결과이다. 평균 성충 수, 겨울 최소치, 개화기 채집벌일(`ΣF_t`), 필요한 벌통 수를 혼동하지 않는다. 현재 모델의 `foragerDays`는 출력기간 전체의 누적값이며 자동으로 개화기로 제한한 값이 아니다. 올림된 정수 벌통 수는 작은 변화에도 같게 보일 수 있으므로 연속적인 방문 능력도 함께 확인한다. 한 변수씩 바꾸는 분석만으로 변수 간 상호작용이나 전역 민감도를 결론 내리지 않는다.

최대 산란량과 단계 생존율은 성충 유입량에 곱셈으로 작용하므로 민감도가 거의 같게 나올 수 있다. 작은 수치 차이를 근거로 어느 생물학적 요인이 확실히 가장 중요하다고 선언하지 않는다. 관측 성충 수만으로 이런 파라미터들을 각각 추정하기 어려운 구조적 식별 문제도 있다.

현재 국소 분석은 최대 산란량, 수정란 비율, 알 생존율, 유충·번데기 생존율, 성수기 채집 기대수명, 월동 기대수명, 전환 연령 기준, 양육 반포화 개체수의 8개 변수를 비교한다. 기본 변화폭은 ±10%이며 물리적 범위에서 제한한다. 정렬은 정규화 민감도의 절댓값을 기준으로 한다. 영양·사회적 억제·계절 위상·초기조건은 이 순위에 포함되지 않으므로 모든 요인을 비교한 결과라고 표현하지 않는다.

별도의 불확실성 기능은 같은 8개 변수를 기준값 주변의 독립 균등분포에서 동시에 추출한다. 기본 64회, 시드 2022, 범위 ±10%이며 물리적 범위에서 잘린 구간 내부에 직접 균등 표본을 만든다. 날짜별 P10–P90 밴드는 **가정한 파라미터 시나리오 범위**이며 통계적으로 추정한 신뢰구간이나 날씨 예측구간이 아니다. 표본 수가 작아 극단 시나리오의 확률을 정밀하게 추정하지 않는다. 기준값 0인 변수는 이 상대 범위 표본에서 0으로 유지된다.

대회 보고서를 확장할 때는 주요 변수 쌍의 격자 실험을 추가하고, 상관된 파라미터를 독립적으로 추출한 가정도 검토한다. 성충 사망이 중요할 가능성은 선행 연구의 동기이며, 현재 모델의 순위는 실제 계산으로 결정해야 한다. [Rumkee et al. 2015](https://pmc.ncbi.nlm.nih.gov/articles/PMC4633771/)

## 7. 수분용 벌통 수의 해석

부지 면적은 문제의 근사값인 81,000 m²를 기준으로 한다. 작물 종류만 선택한다고 꽃 수가 실측되는 것은 아니다. 꽃 밀도는 **그날 수분 대상인 꽃/m²**, 꽃당 요구 방문량은 **유효 방문/꽃/일**이다. 개화일마다 같은 밀도와 하루 방문 요구량을 적용하므로 개화기간 전체의 누적 꽃 밀도를 넣으면 수요가 중복 계산된다. 개화 날짜와 기간, 날씨에 따른 활동 비율, 대상 작물을 선택하는 비율, 유효 방문 비율은 각각 별도의 입력이다. 같은 꽃을 반복 방문하는 효과와 수정 성공률을 단순화하므로 출력의 단위는 **유효 방문 상당량**이다.

```text
D_t = area × flowerDensity × visitsPerFlower
      × (1−naturalShare) × (1+reserve)
C_t = F_t × A(t) × weather × cropShare × visitsPerBee × efficiency
```

기본 예시는 `area=81000`, `bloomStart=120`, `bloomDays=21`, `flowerDensity=30`, `visitsPerFlower=2`, `visitsPerBee=1200`, `weather=0.7`, `cropShare=0.5`, `efficiency=0.5`, `naturalShare=0`, `reserve=0.1`이다. 모두 모형 탐색용 값이며 특정 작물의 실측 조합이 아니다. 여러 해를 출력하더라도 수분 계산은 첫 번째 출력년의 날짜를 사용한다. 연말을 넘는 개화 구간은 그 한 해의 계절곡선을 반복한 것으로 처리한다.

날짜 `t`의 꽃 방문 수요를 `D_t`, 한 벌통 공급을 `C_t`라고 하면 누적량만 충족하는 벌통 수의 하한은 `ceil(ΣD_t / ΣC_t)`이다. 이 계산은 뒤늦은 방문이 이전 날짜의 부족을 보완할 수 있다고 가정한다. 매일 필요한 방문을 충족시키려면 `ceil(max_t(D_t / C_t))`가 필요하다. 수요가 양수인데 공급이 0인 날짜는 벌통 수를 아무리 늘려도 이 가정 아래에서는 충족하지 못한다.

두 숫자의 차이는 개화와 채집 능력의 시점 불일치를 보여 준다. 하루별 제약은 꽃의 실제 수분 가능 기간을 하루로 단순화한 보수적 조건이다. 개화가 여러 날 지속되는 개별 꽃의 수용 기간을 추적하면 이 제약을 더 현실적으로 개선할 수 있다. 벌통 수에 곱하는 예비율은 운영 가정이며 통계적 보장확률이 아니다.

지역 비교값은 별도로 제시한다. OSU의 태평양 북서부 평균 사용량은 사과 0.5, 체리 2, 블루베리 3벌통/에이커이다. 20에이커의 환산값은 각각 10·40·60벌통이지만 실제 농장의 확정 권장량은 아니다. 모델과 차이가 크면 꽃 밀도, 대상 작물 방문 비중, 군집 강도, 개화기의 정의를 검토한다. 경험값에 맞추려고 설명 없이 파라미터를 바꾸지 않는다. [OSU Extension, PNW 623](https://extension.oregonstate.edu/catalog/pnw-623-evaluating-honey-bee-colonies-pollination)

20에이커를 정사각형으로 가정하면 한 변이 약 285m이고 중심에서 모서리까지 약 201m이다. 이는 모양을 정사각형으로 가정한 기하학적 예시일 뿐이다. 면적만으로 부지의 최대 거리를 알 수는 없다. 기본 수분 모델은 벌통 배치, 주변 꽃과의 경쟁, 벌통 간 자원 경쟁을 공간적으로 계산하지 않는다.

## 8. 현재 한계와 우선 확장

먹이 저장량과 꽃가루의 단백질 성분, 기생충·질병의 상태, 농약 노출, 여왕 교체, 분봉을 명시적으로 추적하지 않는다. H에서 F로의 모집과 수벌 생존에는 가정한 함수가 포함된다. 같은 날짜의 모든 벌에 계절 사망확률을 적용하므로 개체의 생리적 월동 상태를 완전히 표현하지 못한다. 모든 벌통의 공급을 같은 배수로 늘리는 수분 계산은 많은 벌통을 놓았을 때의 자원 경쟁을 생략한다.

다음 연구 단계의 우선순위는 (1) 작물·지역별 개화와 꽃 밀도 자료, (2) 군집 관측자료에 대한 보정·검증, (3) 파라미터 상호작용과 불확실성, (4) 먹이·영양과 공간 경쟁 확장이다. 현 버전보다 많은 상태변수를 만드는 것 자체를 성능 개선으로 취급하지 않는다.

## 9. 25페이지 보고서 배분 예시

| 내용 | 페이지 |
|---|---:|
| 요약시트 | 1 |
| 목차 | 1 |
| 문제 해석·가정·변수 | 2 |
| 개체군 모델 | 5 |
| 결과와 검증 | 3 |
| 민감도 분석 | 4 |
| 수분 모델 | 3 |
| 한계와 결론 | 1 |
| 비전문가용 블로그 | 1 |
| 참고문헌 | 2 |
| 재현을 위한 부록 | 2 |
| 합계 | 25 |

이는 자체 편집 제안이며 대회가 지정한 절별 분량은 아니다. 그래프마다 설정·단위·기간을 적고 실험 설정 JSON과 일별 결과를 함께 보관한다. 블로그의 숫자는 선택한 최종 시나리오와 일치시킨다.

## 참고문헌

1. COMAP. (2022). [2022 HiMCM Problem A](https://www.contest.comap.org/highschool/contests/himcm/2022_Problems/2022_HiMCM_Problem_A.pdf).
2. Khoury, D. S., Myerscough, M. R., & Barron, A. B. (2011). [A Quantitative Model of Honey Bee Colony Population Dynamics](https://doi.org/10.1371/journal.pone.0018491). PLOS ONE, 6(4), e18491.
3. Khoury, D. S., Barron, A. B., & Myerscough, M. R. (2013). [Modelling Food and Population Dynamics in Honey Bee Colonies](https://doi.org/10.1371/journal.pone.0059084). PLOS ONE, 8(5), e59084.
4. Fukuda, H., & Sakagami, S. F. (1968). [Worker brood survival in honeybees](https://doi.org/10.1007/BF02514731). Population Ecology, 10, 31–39.
5. FAO. [Honeybee biology](https://www.fao.org/4/x0083e/X0083E03.htm).
6. Rumkee, J. C. O., Becher, M. A., Thorbek, P., Kennedy, P. J., & Osborne, J. L. (2015). [Predicting Honeybee Colony Failure: Using the BEEHAVE Model to Simulate Colony Responses to Pesticides](https://pmc.ncbi.nlm.nih.gov/articles/PMC4633771/). Environmental Science & Technology, 49(21), 12879–12887.
7. Oregon State University Extension Service. [Evaluating honey bee colonies for pollination](https://extension.oregonstate.edu/catalog/pnw-623-evaluating-honey-bee-colonies-pollination). PNW 623.
